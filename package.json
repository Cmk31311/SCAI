{
  "name": "scai",
  "version": "1.0.0",
  "description": "SCAI – Where vision becomes understanding. Landing page for an AI-powered screen intelligence platform.",
  "scripts": {
    "build": "node scripts/build.js",
    "update-release": "node scripts/fetch-latest-release.js && npm run build",
    "dev": "npm run build"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1"
  }
}
